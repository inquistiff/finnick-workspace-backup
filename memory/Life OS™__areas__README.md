# Areas
Numbered subfolders (01–08) house the major domains of life/work. Each folder keeps:
- a folder note (`index.md` or top-level note) describing the domain
- project/asset subfolders specific to that domain

Use the numeric prefixes to keep the order stable when listing from the CLI.
